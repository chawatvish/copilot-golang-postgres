# Server Configuration
PORT=8080
GIN_MODE=release

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_username
DB_PASSWORD=your_password
DB_NAME=gin_simple_db
DB_SSLMODE=disable
DB_TIMEZONE=UTC

# Application Configuration
# Set to false to use in-memory storage instead of database
USE_DATABASE=true
