# Server Configuration
PORT=8080
GIN_MODE=release

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_username
DB_PASSWORD=your_password
DB_NAME=gin_simple_db
DB_SSLMODE=disable
DB_TIMEZONE=UTC

# Application Configuration
# Set to false to use in-memory storage instead of database
USE_DATABASE=true

# JWT Authentication Configuration
JWT_SECRET=your-secret-key-change-this-in-production-make-it-long-and-random
JWT_EXPIRE_HOUR=24

# For production, make sure to:
# 1. Use a strong, randomly generated JWT_SECRET (at least 32 characters)
# 2. Set GIN_MODE=release
# 3. Use proper database credentials
# 4. Consider using environment-specific configuration files
